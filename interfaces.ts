export interface IBody {
    title: string
    body: string
}

export interface IParams {
    id: string
}

export interface INote {
    id: string
    title: string
    body: string
    lastEdited: number     
}